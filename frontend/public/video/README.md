# Video folder (unused)

The app no longer uses a background video; the UI relies on gradient styling only.

You can keep this folder empty or delete it. Vite will still copy this README into `dist/video/` on build if the folder exists.
